MakeDeck() returns a list containing {Set:Rank} pairs

ShuffleDeck() returns whatever list it is given, but shuffled

DealCard() returns `Card`({Set:Rank} pair from `Deck[0]`) and `Deck`(With `Card` Removed)